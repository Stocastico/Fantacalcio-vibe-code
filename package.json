{
  "name": "fantacalcio-vibe-code",
  "version": "2026.8.0",
  "private": true,
  "description": "Helper offline per l'asta del fantacalcio e per il mercato di riparazione",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "build": "node tools/build-standalone.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
